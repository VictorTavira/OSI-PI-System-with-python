# OSI-PI-System-with-python

We need to connect our Jupyter notebook in local (anaconda) with a OSI PI System, but an error like this appears: {'Message': 'Authorization has been denied for this request.'}
